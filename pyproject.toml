[project]
name = "nmead"
version = "0.2.0"
description = "Read GPS/NMEA position from various sources (rigctld, GPS2IP) and forward to sinks (console, WSJT-X)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pynmea2>=1.19.0",
]

[project.scripts]
nmead = "main:main"
