[project]
name = "nmead"
version = "0.1.0"
description = "Read GPS/NMEA data from IC-705 via rigctld (Hamlib)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pynmea2>=1.19.0",
]

[project.scripts]
nmead = "main:main"
